extends layout

block content

  div(id="root" style="width: 280px; margin: 40px auto; padding: 10px;")
  meta(name='viewport', content='width=device-width, initial-scale=1.0')

  script.
    var lock = new Auth0Lock('j0ICHuTbfDJXIrcQQBNyse42Ec48ul0E', 'brewbase.auth0.com',{
          auth: {
          redirectUrl: 'http://localhost:4000/callback'
        , responseType: 'code'
        , params: {
          scope: 'openid name email picture'
        }}
        , languageDictionary: {
                title: "BREWBASE"
        }
        , theme: {
          logo:            'https://trello-attachments.s3.amazonaws.com/589e0a2e824848cfcc5123a1/1024x1024/9b268254a0c1cc0a50ce380fda60510d/brewbaselogo.png',
          primaryColor:    '#2b2b28'
        }
        , closable: false
        , socialButtonStyle: 'small'
        });


    // Show lock's login widget
    lock.show();
