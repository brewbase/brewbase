import React from 'react'
import DesktopNavBar from '../components/DesktopNavBar.js'
import MobileNavBar from '../components/MobileNavBar.js'

const FavoriteBrewsView = (props) => (
    <div>
        <DesktopNavBar />
        <p>Favorite brews view not finished</p>
        <MobileNavBar />
    </div>
)

export default FavoriteBrewsView
